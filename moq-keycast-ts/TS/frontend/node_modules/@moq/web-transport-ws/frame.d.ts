import * as Stream from "./stream.ts";
import { VarInt } from "./varint.ts";
export interface Data {
    type: "stream";
    id: Stream.Id;
    data: Uint8Array;
    fin: boolean;
}
export interface ResetStream {
    type: "reset_stream";
    id: Stream.Id;
    code: VarInt;
}
export interface StopSending {
    type: "stop_sending";
    id: Stream.Id;
    code: VarInt;
}
export interface ConnectionClose {
    type: "connection_close";
    code: VarInt;
    reason: string;
}
export interface Padding {
    type: "padding";
}
export interface Ping {
    type: "ping";
}
export type Any = Data | ResetStream | StopSending | ConnectionClose;
export declare function encode(frame: Any): Uint8Array;
export declare function decode(buffer: Uint8Array): Any;
