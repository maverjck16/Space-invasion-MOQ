import type * as Path from "../path.ts";
import type { Reader, Writer } from "../stream.ts";
import type { IetfVersion } from "./version.ts";
export declare class TrackStatusRequest {
    #private;
    static id: number;
    trackNamespace: Path.Valid;
    trackName: string;
    constructor({ trackNamespace, trackName }: {
        trackNamespace: Path.Valid;
        trackName: string;
    });
    encode(w: Writer, _version: IetfVersion): Promise<void>;
    static decode(r: Reader, _version: IetfVersion): Promise<TrackStatusRequest>;
}
export declare class TrackStatus {
    #private;
    static id: number;
    trackNamespace: Path.Valid;
    trackName: string;
    statusCode: number;
    lastGroupId: bigint;
    lastObjectId: bigint;
    constructor({ trackNamespace, trackName, statusCode, lastGroupId, lastObjectId, }: {
        trackNamespace: Path.Valid;
        trackName: string;
        statusCode: number;
        lastGroupId: bigint;
        lastObjectId: bigint;
    });
    encode(w: Writer, _version: IetfVersion): Promise<void>;
    static decode(r: Reader, _version: IetfVersion): Promise<TrackStatus>;
    static readonly STATUS_IN_PROGRESS = 0;
    static readonly STATUS_NOT_FOUND = 1;
    static readonly STATUS_NOT_AUTHORIZED = 2;
    static readonly STATUS_ENDED = 3;
}
//# sourceMappingURL=track.d.ts.map