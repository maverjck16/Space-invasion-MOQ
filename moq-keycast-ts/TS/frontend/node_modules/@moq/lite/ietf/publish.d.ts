import type * as Path from "../path.ts";
import type { Reader, Writer } from "../stream.ts";
import { type IetfVersion } from "./version.ts";
export declare class Publish {
    #private;
    static id: number;
    requestId: bigint;
    trackNamespace: Path.Valid;
    trackName: string;
    trackAlias: bigint;
    groupOrder: number;
    contentExists: boolean;
    largest: {
        groupId: bigint;
        objectId: bigint;
    } | undefined;
    forward: boolean;
    constructor({ requestId, trackNamespace, trackName, trackAlias, groupOrder, contentExists, largest, forward, }: {
        requestId: bigint;
        trackNamespace: Path.Valid;
        trackName: string;
        trackAlias: bigint;
        groupOrder: number;
        contentExists: boolean;
        largest: {
            groupId: bigint;
            objectId: bigint;
        } | undefined;
        forward: boolean;
    });
    encode(w: Writer, version: IetfVersion): Promise<void>;
    static decode(r: Reader, version: IetfVersion): Promise<Publish>;
}
export declare class PublishOk {
    #private;
    static id: number;
    encode(w: Writer, _version: IetfVersion): Promise<void>;
    static decode(r: Reader, _version: IetfVersion): Promise<PublishOk>;
}
export declare class PublishError {
    #private;
    static id: number;
    requestId: bigint;
    errorCode: number;
    reasonPhrase: string;
    constructor({ requestId, errorCode, reasonPhrase, }: {
        requestId: bigint;
        errorCode: number;
        reasonPhrase: string;
    });
    encode(w: Writer, _version: IetfVersion): Promise<void>;
    static decode(r: Reader, _version: IetfVersion): Promise<PublishError>;
}
export declare class PublishDone {
    #private;
    static readonly id = 11;
    requestId: bigint;
    statusCode: number;
    reasonPhrase: string;
    constructor({ requestId, statusCode, reasonPhrase, }: {
        requestId: bigint;
        statusCode: number;
        reasonPhrase: string;
    });
    encode(w: Writer, _version: IetfVersion): Promise<void>;
    static decode(r: Reader, _version: IetfVersion): Promise<PublishDone>;
}
//# sourceMappingURL=publish.d.ts.map