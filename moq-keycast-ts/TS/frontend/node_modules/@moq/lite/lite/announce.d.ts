import * as Path from "../path.ts";
import type { Reader, Writer } from "../stream.ts";
import { Version } from "./version.ts";
export declare class Announce {
    #private;
    suffix: Path.Valid;
    active: boolean;
    hops: number;
    constructor(props: {
        suffix: Path.Valid;
        active: boolean;
        hops?: number;
    });
    encode(w: Writer, version: Version): Promise<void>;
    static decode(r: Reader, version: Version): Promise<Announce>;
    static decodeMaybe(r: Reader, version: Version): Promise<Announce | undefined>;
}
export declare class AnnounceInterest {
    #private;
    prefix: Path.Valid;
    constructor(prefix: Path.Valid);
    encode(w: Writer): Promise<void>;
    static decode(r: Reader): Promise<AnnounceInterest>;
}
export declare class AnnounceInit {
    #private;
    suffixes: Path.Valid[];
    constructor(paths: Path.Valid[]);
    encode(w: Writer, version: Version): Promise<void>;
    static decode(r: Reader, version: Version): Promise<AnnounceInit>;
}
//# sourceMappingURL=announce.d.ts.map