import type { Reader, Writer } from "../stream.ts";
import { Version } from "./version.ts";
export declare class Extensions {
    entries: Map<bigint, Uint8Array>;
    constructor();
    set(id: bigint, value: Uint8Array): void;
    get(id: bigint): Uint8Array | undefined;
    remove(id: bigint): Uint8Array | undefined;
    encode(w: Writer): Promise<void>;
    static decode(r: Reader): Promise<Extensions>;
}
export declare class SessionClient {
    #private;
    versions: number[];
    extensions: Extensions;
    constructor(versions: number[], extensions?: Extensions);
    encode(w: Writer): Promise<void>;
    static decode(r: Reader): Promise<SessionClient>;
}
export declare class SessionServer {
    #private;
    version: number;
    extensions: Extensions;
    constructor(version: number, extensions?: Extensions);
    encode(w: Writer): Promise<void>;
    static decode(r: Reader): Promise<SessionServer>;
}
export declare class SessionInfo {
    #private;
    bitrate: number;
    constructor(bitrate: number);
    encode(w: Writer, version: Version): Promise<void>;
    static decode(r: Reader, version: Version): Promise<SessionInfo>;
    static decodeMaybe(r: Reader, version: Version): Promise<SessionInfo | undefined>;
}
//# sourceMappingURL=session.d.ts.map