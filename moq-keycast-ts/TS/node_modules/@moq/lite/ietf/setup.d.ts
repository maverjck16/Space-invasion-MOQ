import type { Reader, Writer } from "../stream.ts";
import { Parameters } from "./parameters.ts";
import { type IetfVersion } from "./version.ts";
export declare class ClientSetup {
    #private;
    static id: number;
    versions: number[];
    parameters: Parameters;
    constructor({ versions, parameters }: {
        versions: number[];
        parameters?: Parameters;
    });
    encode(w: Writer, version: IetfVersion): Promise<void>;
    static decode(r: Reader, version: IetfVersion): Promise<ClientSetup>;
}
export declare class ServerSetup {
    #private;
    static id: number;
    version: number;
    parameters: Parameters;
    constructor({ version, parameters }: {
        version: number;
        parameters?: Parameters;
    });
    encode(w: Writer, version: IetfVersion): Promise<void>;
    static decode(r: Reader, version: IetfVersion): Promise<ServerSetup>;
}
//# sourceMappingURL=setup.d.ts.map