import type * as Path from "../path.ts";
import type { Reader, Writer } from "../stream.ts";
import { type IetfVersion } from "./version.ts";
export declare class PublishNamespace {
    #private;
    static id: number;
    requestId: bigint;
    trackNamespace: Path.Valid;
    constructor({ requestId, trackNamespace }: {
        requestId: bigint;
        trackNamespace: Path.Valid;
    });
    encode(w: Writer, version: IetfVersion): Promise<void>;
    static decode(r: Reader, version: IetfVersion): Promise<PublishNamespace>;
}
export declare class PublishNamespaceOk {
    #private;
    static id: number;
    requestId: bigint;
    constructor({ requestId }: {
        requestId: bigint;
    });
    encode(w: Writer, _version: IetfVersion): Promise<void>;
    static decode(r: Reader, _version: IetfVersion): Promise<PublishNamespaceOk>;
}
export declare class PublishNamespaceError {
    #private;
    static id: number;
    requestId: bigint;
    errorCode: number;
    reasonPhrase: string;
    constructor({ requestId, errorCode, reasonPhrase, }: {
        requestId: bigint;
        errorCode: number;
        reasonPhrase: string;
    });
    encode(w: Writer, _version: IetfVersion): Promise<void>;
    static decode(r: Reader, _version: IetfVersion): Promise<PublishNamespaceError>;
}
export declare class PublishNamespaceCancel {
    #private;
    static id: number;
    trackNamespace: Path.Valid;
    requestId: bigint;
    errorCode: number;
    reasonPhrase: string;
    constructor({ trackNamespace, errorCode, reasonPhrase, requestId, }?: {
        trackNamespace?: Path.Valid;
        errorCode?: number;
        reasonPhrase?: string;
        requestId?: bigint;
    });
    encode(w: Writer, version: IetfVersion): Promise<void>;
    static decode(r: Reader, version: IetfVersion): Promise<PublishNamespaceCancel>;
}
export declare class PublishNamespaceDone {
    #private;
    static readonly id = 9;
    trackNamespace: Path.Valid;
    requestId: bigint;
    constructor({ trackNamespace, requestId, }?: {
        trackNamespace?: Path.Valid;
        requestId?: bigint;
    });
    encode(w: Writer, version: IetfVersion): Promise<void>;
    static decode(r: Reader, version: IetfVersion): Promise<PublishNamespaceDone>;
}
//# sourceMappingURL=publish_namespace.d.ts.map