export declare class Stream {
    reader: Reader;
    writer: Writer;
    constructor(props: {
        writable: WritableStream<Uint8Array>;
        readable: ReadableStream<Uint8Array>;
    });
    static accept(quic: WebTransport): Promise<Stream | undefined>;
    static open(quic: WebTransport, priority?: number): Promise<Stream>;
    close(): void;
    abort(reason: Error): void;
}
export declare class Reader {
    #private;
    constructor(stream: ReadableStream<Uint8Array>, buffer?: Uint8Array);
    constructor(stream: undefined, buffer: Uint8Array);
    read(size: number): Promise<Uint8Array>;
    readAll(): Promise<Uint8Array>;
    string(): Promise<string>;
    bool(): Promise<boolean>;
    u8(): Promise<number>;
    u16(): Promise<number>;
    u53(): Promise<number>;
    u62(): Promise<bigint>;
    done(): Promise<boolean>;
    stop(reason: unknown): void;
    get closed(): Promise<void>;
}
export declare class Writer {
    #private;
    constructor(stream: WritableStream<Uint8Array>);
    bool(v: boolean): Promise<void>;
    u8(v: number): Promise<void>;
    u16(v: number): Promise<void>;
    i32(v: number): Promise<void>;
    u53(v: number): Promise<void>;
    u62(v: bigint): Promise<void>;
    write(v: Uint8Array): Promise<void>;
    string(str: string): Promise<void>;
    close(): void;
    get closed(): Promise<void>;
    reset(reason: unknown): void;
    static open(quic: WebTransport): Promise<Writer>;
}
export declare class Readers {
    #private;
    constructor(quic: WebTransport);
    next(): Promise<Reader | undefined>;
    close(): void;
}
//# sourceMappingURL=stream.d.ts.map